function homePage(){
  const m=data.metrics,r=score(),agenda=upcoming(),pending=data.priorities.filter(x=>!x.done).length;
  return `<div class="grid hero-grid">
    <section class="card brief-card"><div class="brief-content"><div><div class="eyebrow">Morning Briefing</div><h2>${esc(data.briefing.headline)}</h2><p class="copy">${esc(data.briefing.summary)}</p><div class="brief-meta"><div><small>TOP PRIORITY</small><b>${esc(data.briefing.mission)}</b><span>${pending} items still open</span></div><div><small>TODAY'S FOCUS</small><b>${esc(data.briefing.focus)}</b><span>Keep the plan realistic</span></div><div><small>DAILY NOTE</small><b>${esc(data.briefing.mindset)}</b><span>Adjust as the day changes</span></div></div></div><div class="ring" style="--score:${r}"><div><span><small>READINESS</small><b>${r}</b><em>${statusFor(r).toUpperCase()}</em></span></div></div></div></section>
    <section class="card agenda-card"><div class="card-title"><h3>Today's Schedule</h3><span onclick="showPage('calendar')" style="cursor:pointer">Open calendar</span></div><div class="agenda-list">${agenda.map((e,i)=>`<div class="agenda-item"><time>${esc(toDisplayTime(e.start))}</time><div><b>${esc(e.title)}</b><span>${esc(e.category||'General')}</span></div><i class="agenda-line ${i===2?'gold':''}"></i></div>`).join('')||'<div class="empty">No events scheduled.</div>'}</div></section>
  </div>

  ${section('Your Morning','Schedule, priorities, health, and career')}
  <div class="grid morning-overview-grid">
    <section class="card"><div class="card-title"><h3>Today's Priorities</h3><span>${pending} remaining</span></div><div class="overview-list">${data.priorities.map((p,i)=>`<div class="overview-item"><input type="checkbox" ${p.done?'checked':''} onchange="togglePriority(${i},this.checked)"><div><b>${esc(p.title)}</b><span>${esc(p.category)}</span></div><time>${esc(p.time)}</time></div>`).join('')}</div><button class="btn" style="margin-top:11px" onclick="showPage('command')">Open today's view</button></section>
    <section class="card"><div class="card-title"><h3>Health & Recovery</h3><span>${statusFor(r)}</span></div><div class="soft-stat-grid"><div class="soft-stat"><small>Sleep</small><b>${m.sleep.toFixed(1)} h</b><em>${m.sleep>=7?'On target':'Needs attention'}</em></div><div class="soft-stat"><small>Resting HR</small><b>${m.restingHr} bpm</b><em>Current</em></div><div class="soft-stat"><small>HRV</small><b>${m.hrv} ms</b><em>Balanced</em></div><div class="soft-stat"><small>Water</small><b>${m.water} oz</b><em>${Math.max(0,m.waterGoal-m.water)} oz remaining</em></div></div><button class="btn" style="margin-top:11px" onclick="showPage('heart')">View health details</button></section>
    <section class="card"><div class="card-title"><h3>Career Progress</h3><span>${data.career.length} active areas</span></div><div class="list">${data.career.slice(0,3).map(c=>`<div class="list-row"><div class="role"><i class="badge ${c.color||'gold'}"></i><div><b>${esc(c.role)}</b><span>${esc(c.status)} • ${esc(c.next)}</span></div></div><b>${c.progress}%</b></div>`).join('')}</div><button class="btn" style="margin-top:11px" onclick="showPage('career')">Open career view</button></section>
  </div>

  ${section('Morning Briefing','Your eight 7:00 AM briefing topics')}
  <div class="briefing-priorities">${data.briefingPriorities.map(x=>`<span class="brief-priority-chip">${esc(x)}</span>`).join('')}</div>
  <div class="grid brief-topic-grid">${data.morningTopics.map(t=>`<section class="card brief-topic"><div class="topic-header"><span class="topic-icon">${esc(t.icon)}</span><div><b>${esc(t.title)}</b><small>${esc(t.priority)}</small></div></div><h3>${esc(t.headline)}</h3><p>${esc(t.summary)}</p><div class="topic-impact"><strong>Why it matters</strong>${esc(t.impact)}</div></section>`).join('')}</div>
  <div class="brief-source-note">This screen now mirrors the subjects in your scheduled morning briefing: Law Enforcement & Public Safety, Maryland News, National Politics, Markets & Economy, Fitness & Health, Technology & AI, Tesla & EVs, and Firearms & 2A. The current static build stores editable summaries locally; live stories can be connected in a later phase.</div>

  ${section('Performance Snapshot','Current local values')}
  <section class="card perf-band"><div class="perf-block"><small>RESTING HR</small><strong>${m.restingHr} bpm</strong><em>Baseline</em>${spark([61,60,62,59,60,58,58])}</div><div class="perf-block"><small>HRV</small><strong>${m.hrv} ms</strong><em>Balanced</em>${spark([52,55,53,58,57,61,m.hrv])}</div><div class="perf-block"><small>SLEEP</small><strong>${m.sleep.toFixed(1)} h</strong><em>${m.sleep>=7?'On target':'Needs attention'}</em><div class="sleep-bars">${[55,76,63,82,91,72,88,65,94,78,84,89].map(x=>`<i style="height:${x}%"></i>`).join('')}</div></div><div class="perf-block"><small>READINESS DETAILS</small><div class="breakdown">${[['Sleep Quality',Math.min(100,m.sleep/8*100)],['HRV Balance',Math.min(100,m.hrv/70*100)],['Training Load',90],['Recovery',r]].map((x,i)=>`<div class="break-row"><span>${x[0]}</span><div class="bar ${i===1?'gold':''}"><i style="width:${Math.round(x[1])}%"></i></div><b>${Math.round(x[1])}</b></div>`).join('')}</div></div></section>

  ${section('Planning & Progress','Nutrition, important dates, and shortcuts')}
  <div class="grid three">
    <section class="card"><div class="card-title"><h3>Nutrition Summary</h3><span onclick="showPage('nutrition')" style="cursor:pointer">Open</span></div><div class="nutrition-wrap"><div class="mini-ring" style="--p:${Math.min(100,m.calories/m.calorieGoal*100)}"><div><span><b>${m.calories.toLocaleString()}</b><small>/ ${m.calorieGoal.toLocaleString()} kcal</small></span></div></div><div>${[['Protein',m.protein,m.proteinGoal,'g'],['Carbs',m.carbs,m.carbGoal,'g'],['Fat',m.fat,m.fatGoal,'g']].map(x=>`<div class="macro"><span>${x[0]}</span><b>${x[1]}${x[3]} / ${x[2]}${x[3]}</b></div>`).join('')}</div></div><div class="macro" style="margin-top:13px"><span>Water</span><b>${m.water} oz / ${m.waterGoal} oz</b></div><div class="bar"><i style="width:${Math.min(100,m.water/m.waterGoal*100)}%"></i></div></section>
    <section class="card"><div class="card-title"><h3>Important Dates</h3><span>Coming up</span></div>${data.deadlines.slice(0,3).map(d=>`<div class="deadline"><div class="date-box"><small>${esc(d.month)}</small><b>${esc(d.day)}</b></div><div><strong>${esc(d.type)}</strong><span>${esc(d.title)}</span></div><em>${esc(d.remaining)}</em></div>`).join('')}</section>
    <section class="card"><div class="card-title"><h3>Quick Links</h3><span>Common actions</span></div><div class="quick-grid"><button class="quick" onclick="showPage('calendar')">◫ Add Event</button><button class="quick" onclick="showPage('command')">✓ Priorities</button><button class="quick" onclick="showPage('briefing')">≡ Edit Brief</button><button class="quick" onclick="showPage('strength')">◎ Workouts</button><button class="quick" onclick="showPage('nutrition')">◉ Nutrition</button><button class="quick" onclick="exportData()">⇩ Backup</button></div></section>
  </div>`
}

function commandPage(){const done=data.priorities.filter(x=>x.done).length;return `<div class="grid two"><section class="card brief-card"><div class="eyebrow">Today's Overview</div><h2>${esc(data.briefing.headline)}</h2><p class="copy">${esc(data.briefing.summary)}</p><div class="brief-meta"><div><small>PROGRESS</small><b>${done} of ${data.priorities.length}</b><span>Priorities completed</span></div><div><small>TODAY'S FOCUS</small><b>${esc(data.briefing.focusMode)}</b><span>${esc(data.briefing.focus)}</span></div><div><small>READINESS</small><b>${score()} / 100</b><span>${statusFor(score())}</span></div></div></section><section class="card"><div class="card-title"><h3>Today's Priorities</h3><span>${done}/${data.priorities.length} complete</span></div>${data.priorities.map((p,i)=>`<div class="mission ${p.done?'done':''}"><input type="checkbox" ${p.done?'checked':''} onchange="togglePriority(${i},this.checked)"><label>${esc(p.title)}</label><small>${esc(p.time)}</small></div>`).join('')}</section></div>${section('Daily Metrics','Performance and progress')}<div class="grid six">${metric('Readiness',score()+'%',statusFor(score()),score())}${metric('Priorities',done+'/'+data.priorities.length,'Completed',done/data.priorities.length*100,true)}${metric('Sleep',data.metrics.sleep.toFixed(1)+' h','Last night',data.metrics.sleep/8*100)}${metric('Protein',data.metrics.protein+' g','Daily target',data.metrics.protein/data.metrics.proteinGoal*100)}${metric('Water',data.metrics.water+' oz','Daily target',data.metrics.water/data.metrics.waterGoal*100)}${metric('Career Areas',data.career.length,'Active',65,true)}</div>`}

function calendarPage(){return `<div class="grid two"><section class="card"><div class="card-title"><h3>Create Event</h3><span>Apple-compatible .ics export</span></div><div class="form"><label class="span2">Title<input id="eventTitle" placeholder="Example: Pull workout"></label><label>Date<input id="eventDate" type="date" value="${today()}"></label><label>Category<select id="eventCategory"><option>Personal</option><option>Training</option><option>Career</option><option>School</option><option>Family</option><option>Home</option></select></label><label>Start<input id="eventStart" type="time" value="17:30"></label><label>End<input id="eventEnd" type="time" value="18:30"></label><label class="span2">Location<input id="eventLocation" placeholder="Optional"></label><label class="span2">Notes<textarea id="eventNotes" placeholder="Preparation, links, or details"></textarea></label></div><div style="display:flex;gap:8px;flex-wrap:wrap;margin-top:12px"><button class="btn gold" onclick="createEvent(true)">Save + Apple Calendar</button><button class="btn" onclick="createEvent(false)">Save locally</button></div><div class="notice" style="margin-top:12px">Apple Calendar support downloads an .ics file. Open it on iPhone or iPad and choose Add to Calendar. This is one-way export, not live two-way sync.</div></section><section class="card"><div class="card-title"><h3>Upcoming Schedule</h3><span>${data.events.length} events</span></div><div class="list">${data.events.map((e,i)=>`<div class="list-row"><div><b>${esc(e.title)}</b><span>${esc(e.date)} • ${esc(toDisplayTime(e.start))} • ${esc(e.category||'General')}</span></div><div><button class="btn" onclick="downloadEvent(${i})">.ics</button> <button class="btn danger" onclick="removeEvent(${i})">×</button></div></div>`).join('')||'<div class="empty">No events saved.</div>'}</div></section></div>`}

function heartPage(){const m=data.metrics;return `${section('Health & Recovery','Current recovery signals')}<div class="grid six">${metric('Resting HR',m.restingHr+' bpm','Current',100-(m.restingHr-45)*2)}${metric('HRV',m.hrv+' ms','Balanced',m.hrv)}${metric('VO₂ Max',m.vo2.toFixed(1),'Current',m.vo2/50*100)}${metric('Sleep',m.sleep.toFixed(1)+' h','Recovery driver',m.sleep/8*100)}${metric('Readiness',score()+'%',statusFor(score()),score())}${metric('Hydration',m.water+' oz','Today',m.water/m.waterGoal*100,true)}</div><div class="grid two" style="margin-top:12px"><section class="card"><div class="card-title"><h3>Recovery Signals</h3><span>7-day trend</span></div>${spark([48,52,54,51,59,61,m.hrv])}</section><section class="card"><div class="card-title"><h3>Today's Guidance</h3><span>Current status</span></div><p class="copy">${score()>=80?'Recovery indicators support normal training intensity. Protect sleep consistency and hydration.':'Reduce total stress and prioritize sleep, hydration, and mobility.'}</p><div class="notice">Health metrics are informational and are not medical advice.</div></section></div>`}

function runningPage(){const m=data.metrics;return `${section('Running Performance','Current training view')}<div class="grid six">${metric('Weekly Mileage',m.weeklyMileage+' mi','Current week',72)}${metric('Longest Run',m.longRun+' mi','Target 8–10',m.longRun/10*100)}${metric('Average Pace',m.avgPace,'Per mile',76,true)}${metric('Run Readiness',Math.max(0,score()-5)+'%','Calculated',score()-5)}${metric('VO₂ Max',m.vo2.toFixed(1),'Aerobic fitness',m.vo2/50*100)}${metric('Recovery',score()+'%','Today',score())}</div><div class="grid two" style="margin-top:12px"><section class="card"><div class="card-title"><h3>Army Ten-Miler</h3><span>Race preparation</span></div><p class="copy"><b style="color:#fff">Projected finish:</b> 1:37:48<br><b style="color:#fff">Goal:</b> 1:35:00<br><b style="color:#fff">Next long run:</b> 8–10 miles</p></section><section class="card"><div class="card-title"><h3>Weekly Structure</h3><span>Suggested framework</span></div><div class="workout"><b>Zone 2 Base</b><span>Easy aerobic work at conversational effort.</span></div><div class="workout"><b>Quality Session</b><span>Tempo, hills, or controlled intervals.</span></div><div class="workout"><b>Long Run</b><span>Build distance gradually while preserving recovery.</span></div></section></div>`}
